# Commutation functions calculations placeholder
# NOTE: Input death table is monthly (Monthlydeathtable.csv) with columns X (month index), l(x) and d(x).
# All commutation functions (Dx, Nx, Cx, Mx) must be calculated on a monthly basis.
